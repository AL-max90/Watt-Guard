services:
  - type: web
    name: watt-guard-backend
    env: python
    plan: free
    buildCommand: "pip install -r backend/requirements.txt"
    startCommand: "gunicorn backend.app:app"
    envVars:
      - key: GEMINI_API_KEY
        sync: false